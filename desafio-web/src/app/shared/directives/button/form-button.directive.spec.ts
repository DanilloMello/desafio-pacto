import { FormButtonDirective } from './form-button.directive';

describe('FormButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FormButtonDirective();
    expect(directive).toBeTruthy();
  });
});
